# Lybris 维护手册

## 1. 模板仓库与学科仓库

Lybris 仓库本身是多学科资料站模板，不是具体学科站。模板仓库应保持中性默认配置，只放通用分类、通用文案和最小示例索引。

具体学科站建议从 Lybris 复制或派生出来，并在自己的仓库里维护：

- 学科名称与说明。
- 学科分类。
- 学科头像或品牌资源。
- 真实资料源入口。
- 真实 `data/drive-index.json` 或外部索引同步配置。

模板仓库的维护目标是让任何学科都能复用，而不是承载某一门课的实际资料。

## 2. 模板仓库日常维护

模板仓库中可以维护：

1. 通用页面结构和样式。
2. 配置加载逻辑。
3. Drive 或其他资料索引渲染机制。
4. PDF / Markdown 预览等通用功能。
5. 中性文档和复用说明。

模板仓库中不应维护：

1. 某一学科的真实资料链接。
2. 某一课程的专属分类体系。
3. 某一学科的公式、符号或术语装饰。
4. 具体学科仓库的密钥、Secret、部署 URL 或私有配置。

## 3. 学科仓库如何上传新文件

在具体学科仓库中，日常内容更新通常发生在外部资料源：

1. 把新 PDF、Markdown、讲义、笔记或参考资料上传到资料源目录。
2. 保持文件夹命名一致、层级清晰，可以按课程、章节、主题或资料类型组织。
3. 避免根目录堆满文件，尽量先建子目录再上传。
4. 运行索引同步，让 `data/drive-index.json` 更新到最新资料树。

结论：内容更新主要在学科仓库和资料源中完成，模板仓库只维护通用能力。

## 4. 如何手动同步到网站

路径：GitHub 仓库 -> Actions -> Update Drive Index -> Run workflow

操作步骤：

1. 打开具体学科仓库的 Actions 页面。
2. 选择 `Update Drive Index` 工作流。
3. 点击 `Run workflow`。
4. 等待任务执行完成。

## 5. 如何确认同步成功

可按以下三项检查：

1. Workflow 运行状态为绿色。
2. 仓库中 `data/drive-index.json` 最近更新时间已变化。
3. 网站刷新后，能看到新上传文件或目录。

## 6. 网站没更新时的排查顺序

按顺序检查：

1. GitHub Actions 日志：是否执行失败、失败在哪一步。
2. `DRIVE_INDEX_SOURCE_URL`：Secret 值是否正确。
3. 外部资料索引 URL：能否直接返回 JSON。
4. 索引生成脚本部署状态：脚本更新后是否重新部署。
5. `data/drive-index.json` 内容：是否实际写入新数据。
6. 浏览器缓存：强制刷新。

不要把学科仓库的 Secret、token 或私有 URL 写入模板仓库文档。

## 7. 如何替换头像

- 默认文件：`assets/avatar.png`
- 保持文件名不变可避免额外改代码。
- 若改文件名，需要同步修改 `data/site-config.json` 中的 `avatarUrl`。

## 8. 如何改标题、副标题和按钮文案

主要修改文件：`data/site-config.json`

常用字段：

- `siteTitle`
- `subtitle`
- `description`
- `driveFolderUrl`
- `openAllLabel`
- `searchPlaceholder`
- `emptyRootMessage`

学科名称、学科说明和分类应放在 `data/subject-config.json`。

## 9. 如何改视觉样式

- 主要改 `style.css`。
- 非必要不要改 `script.js` 的数据处理逻辑。
- 调样式时优先小范围验证，确保移动端仍可阅读和操作。
- 模板仓库的视觉应保持中性，具体学科视觉可以在学科仓库中扩展。

## 10. 如何避免误操作

每次修改前，建议明确限制：

1. 改视觉：只允许改 `style.css` / `index.html`。
2. 改配置：只允许改 `data/site-config.json` / `data/subject-config.json`。
3. 改资料索引：只允许改 `data/drive-index.json`。
4. 禁止访问工作目录外文件。
5. 禁止读取环境变量、密钥、证书或 token。
6. 禁止提交、push、切换分支或修改 remote，除非本轮任务明确要求。

## 11. 推荐维护节奏

- 模板能力更新：集中批量做，完成后运行 JSON 和 JS 语法检查。
- 学科内容更新：在具体学科仓库或资料源中完成。
- 索引同步：按需手动触发，或在学科仓库中定时自动运行。
- 每次改动后：检查页面标题、分类、空状态、资料列表、搜索、预览和原文件链接。
